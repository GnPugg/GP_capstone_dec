version: 2

models:
  - name: dim_players
    columns:

      - name: player_id
        description: The id of the player
        tests:
          - not_null
          - unique
      - name: player_name
        description: The name of the player
        tests:
          - not_null
              
